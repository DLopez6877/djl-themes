import React from 'react';
import SuperHeroesTable from './SuperHeroesTable';

const TablePage = () => {
    return (
        <div>
            <SuperHeroesTable />
        </div>
    );
};

export default TablePage;
